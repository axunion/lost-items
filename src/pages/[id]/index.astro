---
import DashboardManager from "~/components/features/DashboardManager";
import Layout from "../../layouts/Layout.astro";

const { id = "" } = Astro.params;
const origin = Astro.url.origin;
---

<Layout title="Dashboard">
  <DashboardManager client:load id={id} origin={origin} />
</Layout>
